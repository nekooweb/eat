# 2026-09-10 — 公开界面精简

## 范围与依据

用户要求把逻辑/问题写清楚，并删去页面冗余字段。GitHub 基线为 790322ee17e0d4e58a0f46b8762cad854500a9d9。

浏览器和代码审查确认：

- 页面原有64种来源分类标签，含中日同义项、泛化餐厅类别。
- 首页显示「仅 Place ID 待补0」，但该0是public slice内数量，并非完整目录待补量；完整目录有1382条未公开身份。
- 卡片后默认重复显示对比表，全空行也占据空间。
- 页脚包含多段开发者数据流程说明。

## 实现

- displayCuisine 是显示、排除条件、随机分组、卡片、对比及地图文字的共同转换入口，不写回源对象。
- 常见12类优先，其余保持可选并默认折叠；折叠摘要保留已排除项数，按钮带 aria-pressed。
- 泛化类别不显示为筛选/徽章；已知核心业务字段仍保留。
- comparison 用原生 details/summary，默认收起，全空字段行省略。
- 首页统计只显示当前可选店数，移除内部诊断。
- 页脚保留变化风险提示、Hot Pepper / OpenStreetMap 署名和隐私/条款。
- 不删除餐厅数据、不调用采集器、不替换本地主库。
- 辅助JS包体清理仍待行为等价验证；source_facts.js存在运行时菜品清理副作用，本次不直接移除。

## 文档

新增 PIPELINE_ISSUES.md：已证实的数据层缺陷、证据、优先级、后续识别方法和验收边界。
新增 PUBLIC_UI_FIELDS.md：页面保留/折叠/移除规则，以及来源资料不删除的边界。
更新 DEVELOPMENT.md 和 DATA_LOADING_FIELDS.md，修正旧PR状态并链接当前问题清单。

数据层P0/P1仅记录，不宣称随UI一起修复。

## 测试先行

- run 34431188214：初始7项测试1通过、6失败。
- run 34440733793：扩展到10项测试，2通过、8失败。
- 以上均发生在本轮UI实现之前，用于复现；不能视为完成。
- 修复后需通过同义过滤、不变性、随机分组、未知行省略、折叠摘要、输出转义、署名和核心字段测试，再核对真实浏览器操作。

最终验证状态将在完成后追加。
