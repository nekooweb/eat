name: DISABLED - legacy paid full identity collection

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by repository no-paid-data-API policy. Use the frozen ledger and open identity reconciliation.'
