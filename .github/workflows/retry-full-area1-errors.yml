name: DISABLED - legacy paid identity retries

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by repository no-paid-data-API policy. Historical fetch errors stay unresolved unless independent evidence resolves them.'
