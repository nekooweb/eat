name: Build full Area1 inventory ledger

on:
  push:
    branches:
      - bulk-enrichment-pass2
    paths:
      - 'scripts/build_inventory_ledger.mjs'
      - 'scripts/build_inventory_expansion_queue.mjs'
      - '.github/workflows/build-inventory-ledger.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  ledger:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Rebuild canonical production
        run: node scripts/build_production_dataset.mjs
      - name: Build exact 2,804-ID maintenance ledger
        run: node scripts/build_inventory_ledger.mjs data/area1_inventory_ledger.json | tee /tmp/inventory-ledger-summary.json
      - name: Build zero-cost expansion review queue
        run: node scripts/build_inventory_expansion_queue.mjs data/area1_inventory_ledger.json data/area1_inventory_expansion_queue.json | tee /tmp/inventory-expansion-summary.json
      - name: Validate identity and production contracts
        run: |
          node scripts/audit_area1_identity_coverage.mjs
          node scripts/audit_repository.mjs
          node scripts/audit_source_bindings.mjs
          node scripts/audit_normalized_fields.mjs
      - name: Commit generated maintenance range files
        env:
          TARGET_BRANCH: ${{ github.ref_name }}
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/area1_inventory_ledger.json data/area1_inventory_expansion_queue.json
          if git diff --cached --quiet; then
            echo 'Inventory maintenance files unchanged.'
          else
            git commit -m 'Add prioritized Area1 inventory expansion queue'
            git push origin HEAD:$TARGET_BRANCH
          fi
