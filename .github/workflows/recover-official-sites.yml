name: DISABLED - legacy paid official-site recovery

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by repository no-paid-data-API policy. Use persisted/open official sources and trusted locators.'
