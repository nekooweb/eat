name: DISABLED - legacy paid curated verification

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled by repository no-paid-data-API policy. Use independent-source reconciliation.'
