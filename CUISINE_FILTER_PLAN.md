# 餐厅分类与筛选方案

更新日期：2026-09-17。状态：**第一阶段三维分类筛选已在 PR #81 实现并通过当前 CI；待合入 `main` 后进入生产。第二阶段关键词候选尚未启用。**

本方案来自 PR #77，保留其有效设计，但不直接合并已经落后当前 `main` 的旧分支。实际实现基于当前生产主线重新开发，因此不会用旧分支覆盖已经完成的数据、管线和发布修复。

已完成的相关基础修复与第一阶段实现：

- `¥999以下` 与其他预算档统一使用区间重叠语义；
- 公开前端不再携带/绘制 Area1 private reference anchor；
- dish planner 使用中央审查 terminal status + cooldown，避免无变化来源被立即重复派工；
- 已实现 `cuisineStyle` / `foodType` / `venueType` 三个独立维度、固定分类字典、同维度父子关系和分组排除 UI；
- 条件改变后旧随机结果会失效并要求重新生成；未知分类继续保留，不伪造标签；
- 当前维护式 runtime 重建后，1,422 家公开餐厅中 1,198 家可由现有 `cuisine/tags` 精确映射至少一个 accepted 分类，覆盖率 84.25%；其中菜系风格 476 家、主营食物 225 家、店铺类型 576 家，70 家同时命中多个维度；
- 上述覆盖完全来自现有分类字段，不使用店名、菜单或推荐菜关键词推断。

## 1. 目标与现状

目标仍是「少量可选条件 → 三家附近餐厅」，不是建立大型分类检索系统。

原有标签混合了菜系、食物和店铺类型；部分 `tags` 仅重复 `cuisine`。第一阶段把现有已知分类规范化到三个独立维度，以固定概念 ID 驱动筛选，仍不把未知信息补成确定事实。

采用「固定分类字典为主、关键词辅助、模糊项不强行归类」。第一阶段已经落地固定字典；第二阶段关键词只允许产生 candidate，仍需单独审查后才能晋升为 accepted。

## 2. 三个独立维度

| 维度 | 示例 | 规则 |
| --- | --- | --- |
| 菜系风格 `cuisineStyle` | 日式、中餐、台湾料理、韩式、印度料理、泰式、意大利料理 | 表示已知风格，不由单道菜或店型推断 |
| 主营食物 `foodType` | 拉面、乌冬、荞麦面、寿司、咖喱、烤肉、烧鸟、汉堡、汉堡排 | 表示已有分类或证据支持的主营类型，不是全部菜单 |
| 店铺类型 `venueType` | 居酒屋、酒吧、餐酒馆、咖啡店、烘焙店 | 不等同于菜系，也不保证某个时段提供正餐 |

同店可有多个已确认标签，分别放入对应维度。保留原始 `cuisine`、`tags`、ID、名称、来源及历史；分类为派生结果，不覆盖原字段，也不改变生产准入。

每个分类概念使用稳定 ID、中文显示名、所属维度、精确别名和可选父类。每个店铺标签保留以下语义边界：

- `accepted`：现有非泛化分类可直接规范化，或已有经核对的明确经营分类；可用于筛选。
- `candidate`：名称或描述的关键词线索；不可用于硬排除。
- `unresolved`：缺失、复合含义未分清或证据冲突；不补成确定标签。

第一阶段仅消费可确定映射的现有 `cuisine/tags`，不生成 candidate。直接规范化只表示继承现有来源分类，不表示新完成了事实核验。同一内容在 `cuisine` 和 `tags` 重复出现只算一条依据。来源 URL、原文和复核记录留在维护层，不为此增加公开页面字段。

## 3. 同义词与复合类别

先进行 Unicode NFKC、首尾空白和连续空白规范化；英文匹配不区分大小写。使用完整类别词匹配，不做任意子串替换，也不批量转换店铺原名。

以下为第一批字典规则，非全量菜系清单：

| 原词 | 统一显示 | 维度 |
| --- | --- | --- |
| 日式、和食、日本料理 | 日式 | 菜系风格 |
| 中华、中華、中華料理、中餐 | 中餐 | 菜系风格 |
| 台湾菜、台湾料理 | 台湾料理 | 菜系风格 |
| 韩国菜、韓国料理、韩式 | 韩式 | 菜系风格 |
| 拉面、ラーメン、らーめん | 拉面 | 主营食物 |
| 乌冬、乌冬面、うどん、饂飩 | 乌冬 | 主营食物 |
| 荞麦面、蕎麦 | 荞麦面 | 主营食物 |
| 烤肉、烧肉、焼肉 | 烤肉 | 主营食物 |
| 烧鸟、焼鳥、焼き鳥、やきとり | 烧鸟 | 主营食物 |
| 居酒屋 | 居酒屋 | 店铺类型 |
| 酒吧、バー・カクテル | 酒吧 | 店铺类型 |
| 咖啡、咖啡馆、咖啡店、喫茶店 | 咖啡店 | 店铺类型 |

该表仅适用于来源的分类字段；描述中的「咖啡」不自动证明其为咖啡店。

特殊规则：

- `イタリアン・フレンチ` 保留为「意法料理（未细分）」的展示描述，不拆成两项确定菜系，不命中单独的意大利或法国排除项。
- `カフェ・スイーツ` 保留为「咖啡·甜品（未细分）」；仅凭该复合分类不能认定同时具有两种经营属性。
- `焼肉・ホルモン` 保留烤肉／内脏料理的复合信息，不自动生成「主营内脏」标签。
- `餐厅`、`restaurant`、`その他グルメ`、空值记为未知，不生成筛选按钮或无意义徽章。
- `面食` 可归为未细分面类，不能强行当成拉面或荞麦面；`创意料理`、`各国料理` 等不补具体国家风格。
- 尚未进入字典的具体分类保留原展示信息，不因缺少映射删除餐厅；后续按概念维护字典，不自动增加页面按钮。

## 4. 类别关系与排除逻辑

### 4.1 同维度父子关系

仅使用字典中明确登记、无环的同维度关系。例如主营食物的「面类」包含已确认的拉面、乌冬、荞麦面；菜系风格的「中餐」包含已确认的川菜。

- 排除父类时，同时排除命中该父类或其后代的店铺。
- 排除子类时，不排除父类下的其他子类；只标有泛化父类的餐厅也不能被当成该子类。
- 选择父类不会改写子类数据；父子条件同时存在时，餐厅和排除数量均只计一次。

### 4.2 跨维度不自动推导

「日式」与「寿司」不是本方案中的父子关系。「日式＋寿司」均为 `accepted` 的店铺，排除任一项都会被排除；只有寿司标签的店铺，不能仅靠跨维度推断补成日式。

同理，咖喱不自动归入印度料理，拉面不一律归入日式，居酒屋不自动归入日式，烧鸟不自动归入居酒屋。需要跨维度标签时，应分别有分类依据。

### 4.3 组合逻辑

```text
餐厅有效标签 = accepted 标签 + 同维度已登记的祖先标签
分类排除 = 任一已选排除项命中餐厅有效标签
可选餐厅 = 通过现有生产准入与区域边界
           AND 距离通过
           AND 预算通过
           AND NOT 分类排除
```

多个分类排除项之间为 OR；分类、预算、距离三个条件之间为 AND。不选分类排除项时，不增加分类限制。计数按唯一餐厅 ID 去重，不相加重叠标签的数量。

未知或候选标签本身不触发分类排除，但同店其他已确认标签仍正常生效。未知店铺在通过其他条件时可以保留；界面已说明「未分类店铺可能仍在结果中」。不得宣称排除了所有实际上属于该类别的店铺，也不新增默认排除未知项的开关。该功能不提供过敏原或严格忌口保证。

## 5. 关键词辅助规则：第二阶段

第二阶段尚未启用。计划处理顺序为：现有或经核对的明确分类 → 同义词规范化 → 明确经营描述 → 店名关键词候选 → 保留未知。

| 关键词线索 | 候选类型 | 边界 |
| --- | --- | --- |
| 寿司、鮨、すし、sushi | 寿司 | 单字「鱼」不足以判断 |
| カレー、カリー、咖喱、curry | 咖喱 | 不推断印度或日式风格 |
| 焼鳥、焼き鳥、やきとり、yakitori | 烧鸟 | 「鳥」「炭火」单独出现无效 |
| 蕎麦、そば、soba | 荞麦面 | 先识别油そば、焼きそば、まぜそば，不能因包含そば而误判 |
| うどん、饂飩、udon | 乌冬 | 仍区分主营描述和普通菜单 |
| ハンバーガー、burger | 汉堡 | 不与ハンバーグ混合 |
| ハンバーグ、汉堡排、hamburg steak | 汉堡排 | 不因词中包含バー而命中酒吧 |
| インド料理、Indian restaurant | 印度料理 | スパイス、咖喱、地名或品牌联想不足以判断 |

英文使用词边界；日文使用明确词组及排除规则。长词优先只用于消除包含关系误判，不能删除同一名称中另一处独立、有效的类别线索。

店名命中只产生 `candidate`。晋升为 `accepted` 需要准确绑定当前店铺 ID，并核对来源确实描述菜系、主营类型或店型；仅菜单、推荐菜、品牌模板或地址关键词不能作为晋升依据。新证据与已接受分类冲突时先复核，不自动覆盖。第二阶段启用前另做小样本验证，不因第一阶段上线自动执行全量关键词分类。

## 6. 页面交互及既有筛选边界

- 筛选概念来自固定字典，中文显示名与 ID 分离；菜系、主营食物、店型分组，不再随数据新增标签自动长出按钮。
- 保留「常用＋更多」的渐进展示方式；第一阶段未重做整个页面布局，并提供「清除分类排除」。
- 卡片展示与筛选使用同一份规范化分类结果；未确认关键词不展示为已核实徽章。
- 条件变化后更新候选计数，旧结果标为「条件已变化，请重新生成」；仅点击生成才重新抽取。
- 预算档位不变，仍按午餐或晚餐任一已知区间与所选档位重叠判断。`¥999以下` 已在 2026-09-17 基础修复中改为同一重叠规则。
- 未知预算仅在预算不限时可选；距离仍为 300／500／800／1,200 m，沿用现有固定区域距离，不当作用户实时距离或步行距离。
- 候选少于 3 家时提示放宽条件，不静默放宽；不少于 3 家时返回 3 个不同 ID。维持 Web Crypto 随机、百名店既有权重及无评分排名规则；不新增营业中筛选。
- 第一阶段保留现有随机多样性策略；不能把粗粒度父类直接替换成随机分组键，也不能因多标签让同一家店重复入池或获得额外抽样权重。

## 7. 实施状态与轻量验收

第一阶段已在 PR #81 完成：分类字典、三个维度、同维度父子关系、排除匹配、清除操作、候选计数和条件变化反馈。以现有分类为输入，没有为补齐分类启动新采集。

对应阻断测试覆盖：同义词、原字段不可变、同维度祖先关系、跨维度不推断、复合类别不拆分、未知保留、父类排除、子类隔离、清除操作、旧结果失效以及 Pages 资产装配。PR Review、Pages build 与 no-paid-API policy 在当前 head 均通过。

第二阶段仍是后续工作：关键词只生成 candidate、人工核对与冲突处理。无证据的项保持未知；不以覆盖率目标强制填满。两个阶段均不改变后台身份或菜品证据规则。

固定验收语义：

| 样例 | 预期 |
| --- | --- |
| 中華与中餐两个来源别名 | 同一分类 ID、同一排除行为，源值不变 |
| 排除面类；餐厅已知乌冬 | 排除 |
| 排除拉面；餐厅仅知面类或已知乌冬 | 不因该条件排除 |
| 餐厅已知日式＋寿司，分别排除日式或寿司 | 任一条件均排除 |
| 餐厅只知寿司，排除日式 | 不做跨维度推断 |
| 餐厅已知意大利料理＋酒吧，排除任一项 | 排除，餐厅只计一次 |
| 只有イタリアン・フレンチ，排除意大利料理 | 不将复合来源擅自拆分后排除 |
| 咖啡店菜单出现咖喱，排除主营咖喱 | 不仅凭菜单命中排除 |
| 名称为油そば或ハンバーグ | 第一阶段不读店名；第二阶段也必须有消歧规则 |
| 分类未知且其他条件通过 | 保留，不伪造类别或排除保证 |
| 生成后修改距离或分类 | 标记旧结果失效，重新生成才应用新结果 |
| 预算区间为850–1,500，筛选¥999以下 | 按重叠规则通过，不保证整餐低于999 |
| 符合条件的餐厅只有2家／恰好3家 | 提示放宽／返回3个不同ID |

## 8. 文档关系

- [产品需求](REQUIREMENTS.md)：产品边界与基础筛选规则。
- [公开页面字段契约](PUBLIC_UI_FIELDS.md)：公开页面显示行为。
- [开发状态](DEVELOPMENT.md)：区分方案、实现及验证状态。

后续实现第二阶段时继续同步现状文档和测试，不能仅因方案存在就把关键词分类标成完成。
