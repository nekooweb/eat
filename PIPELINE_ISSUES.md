# 数据识别逻辑、问题与修复状态

更新日期：2026-09-10。核查基线：`790322ee17e0d4e58a0f46b8762cad854500a9d9`。

## 统计必须分层

- 全量目录：2,804 个 Place ID。
- 当前主线 public named runtime：1,422 家，未公开 1,382 条。
- 当前主线推荐菜 588 家、特色菜 656 家、任一中文菜品 723 家；集合有重叠，不相加。
- 数据提交、SQLite 构建、生产部署是三个独立状态。上述为核查时主线统计，不证明最后一次网页部署已采用同一提交。

## 已确认的待修问题

| 优先级 | 问题 | 已有证据 | 后续修复与验收 |
| --- | --- | --- | --- |
| P0 | 证据合并按 6 条截断 | merge_google_inventory_detail_evidence.mjs 的 slice(0,6)；6 旧+1 新时旧记录被移除，餐厅计数仍不变 | 证据库保存完整版本；展示数量限制只在导出/页面执行；逐证据保留和显式撤销测试 |
| P0 | 缺失名称变成有效字符串 | reconcile_private_google_hints.py 将缺失 displayName.text 转成字符串 None，非空门禁通过 | 类型/缺失值检查先于字符串转换；重新量化历史影响，不推断所有记录受影响 |
| P0 | 固定公开数量再次出现 | build_reviewed_doutor_chain_gap_recommendations.mjs 写死 1422 | 共用 current catalog/runtime/queue ID 校验，新增店铺不阻断 |
| P1 | 证据提交与发布脱节 | 主线 790322e 推荐菜588，已查部署 bdb48a3 为586 | 指定提交验证→明确发布→线上hash/数量核对；不能依赖bot push递归触发 |
| P1 | 来源实体正确不代表其网站/电话正确 | source_field_quarantine.json 已隔离 Minatoya 个案 | 字段级归属验证；错误网站不导致直接删除正确身份 |
| P1 | 季节菜单缺少完整生命周期 | DOUTOR updater 保存固定审核日期及菜单项 | 明确分店/链级适用范围、复核或有效期；过期与网络失败分开 |
| P2 | 多套报告、文档和旧原型检查滞后 | 部分文档分母和合并状态过期，旧原型校验被允许失败 | 从运行manifest更新当前状态，历史报告明确标注，不降低正式数据门禁 |

以上数据层问题在本次 UI PR 中仅记录，没有修改其生产代码，不能标为已修复。

## 当前识别方案

身份和菜品分开执行：

1. 有名称/地址/电话/官网线索的未知身份：定向核对分店页。
2. 有候选但对应不确定：联合比较别名、分店地址、电话、网站归属。距离/同楼/邮编不能单独证明同店。
3. 只有 Place ID：保留待识别状态，不生成猜测名称。
4. 已确认身份的菜品：官网→留存第三方事实→新独立菜单/PDF来源。
5. 推荐语义与具体菜名同时成立才作为 R；普通真实菜单作为 F；推测和翻译不确定保留 candidate/pending。
6. 先验证独立店、连锁、多分店、错网站和缺失资料样本，再扩大批次。

保存已确认目录名、来源别名和候选名，不互相覆盖。Overture 与其相同上游不能作为两份独立证据。Google 公开详情入口仍需验证；授权保存资料不等于已存在可用的免 Key 批量接口。

当前主线 recommendation gap：官网线索226、留存第三方318、需要新来源290。数量随数据更新；后续从当前队列读取，不写成代码常数。

## 本轮 UI 问题和修复

PR #56 只修改页面呈现及说明，不删除餐厅/source data：

- 64 种原始菜系标签存在同义中日写法，导致重复按钮、排除遗漏和分组伪多样性。统一 display/filter/group mapping，原始对象保持不变。
- 首页错误地以 public placeIdOnly=0 表达待补量；内部真实未公开数量为1382。删除这些开发计数，首页只显示可选店数。
- 卡片与对比表默认重复、全空行占位。对比改为默认折叠，并省略全空字段行。
- 页脚多段内部数据流说明移入开发文档，保留更新时间风险提示、来源署名、隐私和条款。
- 常见菜系默认最多12项，其余仍可展开选择，折叠摘要显示当前排除数量。
- 初始测试仅模拟DOM，不能证明键盘交互和地图可用；合并发布时需要真实浏览器验证。

另一个待后续处理的包体问题：index.html 仍加载 canonical、provenance、source facts、Hot Pepper rich 辅助文件。不能直接删文件，因为 source_facts.js 还带有运行时菜品清理副作用。要先证明 materialized runtime 与移除前后行为等价，再停止浏览器加载；这些文件仍供构建/主库使用。

## 验证状态

初始 PR 测试：run 34431188214，7项中6项失败，复现页面冗余；不是可合并结果。修复后需验证同义排除、三家互异、未知字段省略、资料不变、折叠交互、核心字段、地图导航及署名保留。

本次不发起餐厅采集或 SQLite reset。实际通过的 run、合并及部署状态记录在当天 UI 日志中。
