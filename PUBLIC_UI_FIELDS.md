# 公开页面字段契约

更新日期：2026-09-10。只约束展示/筛选层，不删除主库证据。

## 默认显示

筛选条件（菜系排除、预算、距离）；三家卡片的店名、有效菜系、距离、已有推荐/特色菜、预算、规范化营业时间；位置总览、单店地图和普通导航；可选店数。必要的 OpenStreetMap 和 Hot Pepper 署名、隐私及使用条款继续显示。

未知预算、菜品、时间不生成假值或长占位解释。餐厅/restaurant 等泛化类别不生成筛选按钮或卡片徽章。

## 按需显示

- 默认展示前12种常见菜系，其余放在「更多菜系」中。
- 排除条件保留在内存状态，收起区域仍显示已排除项数；按钮使用 aria-pressed。
- 三店对比默认折叠。字段全未知时不生成对比行；部分已知时仍显示其他店的缺失占位。
- 详细技术计数、字段覆盖率和内部流程说明放到开发文档，不混入用户选择界面。

## 同义类别处理

前端统一中华/中華、韩国菜/韓国料理、台湾菜/台湾料理、乌冬/乌冬面、拉面/ラーメン等。显示、排除条件、随机菜系分组、对比及地图popup共用同一函数，不修改原始来源分类。

复合类目不强制塞进单一菜系；例如イタリアン・フレンチ显示为「意大利·法国菜」，不直接当作只含意大利菜。规则只用于展示和选择，不代表重新核验餐厅类别。

## 删除边界

删除的是默认页面冗余，不是有价值业务数据。原名称、地址、坐标、预算、来源URL、原文及观察历史都保留。主库和公开runtime字段清理另行验收，不能仅根据页面暂不使用就删掉构建依赖。

## 验收

`node --test scripts/test_public_ui_simplification.mjs` 测试字段精简及筛选语义。真实浏览器另查展开/折叠、键盘焦点、筛选后生成、地图和导航。

数据层待修项见 [PIPELINE_ISSUES](PIPELINE_ISSUES.md)，不能把页面精简当作它们已经修复。
